!!! Strict
%html
  %head
    %title= "Mission of Mercy - #{yield(:title) || controller_name.humanize}"
    
    = javascript_include_tag :defaults, 'modalbox.js', 'application_old'
    = javascript_include_tag 'helpers'
    
    = stylesheet_link_tag 'default', :media => :screen
    = stylesheet_link_tag 'modalbox'
    
    /Active Bar
    = javascript_include_tag 'activebar/jquery/jquery-1.3.2.min.js', 'activebar/activebar2.js'
    = yield(:header_js)
    :javascript
      jQuery.noConflict();
      MoM.init('#{form_authenticity_token}');
    
    = yield(:header)
    
  %body{:class => body_css}
    #help_request
    #outer_container  
      #container
        = render :partial => 'users/user_bar'
        #contents
          = yield
        #footer-version
          = "Version #{app_version}"